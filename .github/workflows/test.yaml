on:
  pull_request:
    branches:
      - release
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-go@v3
        with:
          go-version-file: go.mod
      - name: Install Packages for SQLite3 and Cross Compiler
        run: |
          sudo apt update
          sudo apt install -y clang build-essential gcc-aarch64-linux-gnu g++-aarch64-linux-gnu
      - name: Set CGO_ENABLED for SQLite3
        run: echo "CGO_ENABLED=1" >> $GITHUB_ENV
      - name: Install go-sqlite3
        run: go install github.com/mattn/go-sqlite3
      - name: Set up environment for cross-compiling
        run: |
          echo "GOARCH=arm64" >> $GITHUB_ENV
          echo "CC=aarch64-linux-gnu-gcc" >> $GITHUB_ENV
          echo "CXX=aarch64-linux-gnu-g++" >> $GITHUB_ENV
      - run: go test ./... -v
      - uses: goreleaser/goreleaser-action@v4
        with:
          args: release --snapshot
